const {  Monitor } = require("./index");

const array = ["https://functional-emphasized-wallaby.glitch.me/"];
const monitor = new Monitor({ array: array, duration: 5000 });

monitor.start();

setTimeout(() => {
    monitor.end();
}, 9000);

/* Powerd By. 'Roman*/